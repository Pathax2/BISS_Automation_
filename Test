Creation

	•	Engineered TC_13 NR/CISYF end-to-end automation covering the full business flow — herd search, category selection, entity setup, qualification entry, document uploads, declaration, and submission.
	•	Built a self-healing herd recovery engine that autonomously detects submitted herds, re-queries the database, validates agent eligibility, re-authenticates, and resumes execution without manual intervention.

Maintenance

	•	Overhauled ObjectRepository from 13 to 18 sections with 295 DOM-verified locators, full Angular Material-aware scoping, and runtime placeholder injection — zero regressions on all existing test cases.
	•	Conducted live DOM forensics across 12+ page components, identifying and correcting broken locators caused by absent Angular attributes, dynamic IDs, and CDK overlay portal mismatches.

Execution

	•	Resolved Angular Material touch-target interception across checkbox, upload, and radio interactions by standardising JavaScript-based click and state verification patterns.
	•	Replaced all timing-based waits with condition-driven FluentWait, improving execution stability and eliminating false failures caused by Angular’s asynchronous rendering lifecycle.