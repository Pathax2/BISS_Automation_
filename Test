-- Check 1: What columns does VWEN_BISS_CISYF have?
SELECT column_name, data_type
FROM all_tab_columns
WHERE table_name = 'VWEN_BISS_CISYF'
ORDER BY column_id;

-- Check 2: Sample rows including J1994001
SELECT *
FROM vwen_biss_cisyf
WHERE ROWNUM <= 5;
